// Re-export from split files — kept for backwards-compatibility.
// Do NOT add @ccclass classes here; each file may only have one Component.
export { GameSceneBinder } from './GameSceneBinder';
export { ResultSceneBinder } from './ResultSceneBinder';
